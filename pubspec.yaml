name: malbo_hr
description: MALBO HR – HR Management System
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # State & routing
  flutter_riverpod: ^2.5.1
  go_router: ^17.1.0

  # Supabase
  supabase_flutter: ^2.5.6

  # UI & utilities
  google_fonts: ^8.0.1
  intl: ^0.20.2
  uuid: ^4.4.0

  # Responsive helpers
  responsive_framework: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
